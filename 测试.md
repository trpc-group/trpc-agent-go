# replay consistency 实战测试指南

这份文档不是讲概念，而是讲你现在可以怎么做、怎么验证、怎么继续往实装版推进。

## 你现在先做什么

先在仓库根目录跑这一条：

```bash
cd /workspace/trpc-agent-go
go test ./tests/replay_consistency -v
```

如果你看到 `ok`，说明下面这些基础件已经可用：

- 10 条公开 replay case。
- normalizer。
- comparator。
- report 生成器。
- 最小 harness 入口。

现在这套骨架还没有真正连上 Session / Memory 后端，所以它的价值是：先把测试框架、数据模型和报告格式固定住，后面接后端时不会乱。

## 先看你已经有了什么

当前目录是 [tests/replay_consistency](tests/replay_consistency) ，里面已经包含：

1. [cases.go](tests/replay_consistency/cases.go) 里的 10 条公开 case。
1. [normalize.go](tests/replay_consistency/normalize.go) 里的事件、状态、记忆、摘要、track 归一化。
1. [compare.go](tests/replay_consistency/compare.go) 里的 diff 比较。
1. [report.go](tests/replay_consistency/report.go) 里的报告汇总。
1. [harness.go](tests/replay_consistency/harness.go) 里的运行入口。
1. [harness_test.go](tests/replay_consistency/harness_test.go) 里的单测。
1. [session_memory_summary_track_diff_report.json](tests/replay_consistency/session_memory_summary_track_diff_report.json) 里的差异报告样例。

## 现在怎么验证“它是正常的”

### 1. 直接跑单测

```bash
cd /workspace/trpc-agent-go
go test ./tests/replay_consistency -v
```

你重点看这几个断言有没有通过：

- `DefaultReplayCases()` 是否正好返回 10 条。
- `NormalizeSnapshot(...)` 是否会稳定排序。
- `CompareSnapshots(...)` 是否能产出 diff。
- `MarshalReport(...)` 是否能生成合法 JSON。
- `ReplayHarness.Run(...)` 是否能返回报告。

### 2. 只跑这套测试里的某个用例

如果你后面只想盯着某一类逻辑，比如 comparator 或 report，可以这样跑：

```bash
cd /workspace/trpc-agent-go
go test ./tests/replay_consistency -run TestCompareSnapshotsAndBuildReport -v
go test ./tests/replay_consistency -run TestNormalizeSnapshot_SortsAndCanonicalizes -v
```

### 3. 看报告长什么样

打开 [session_memory_summary_track_diff_report.json](tests/replay_consistency/session_memory_summary_track_diff_report.json)，你应该看到这些字段：

- `case_name`
- `backend`
- `path`
- `baseline`
- `actual`
- `allowed_diff`
- `explanation`

这个格式就是你后面真实跑后端时的目标格式。

## 现在怎么用它

### 当前阶段的用法

现在 `ReplayHarness.Run(...)` 还是骨架版，它的作用是先把流程跑通，不是去真的写后端。你可以把它当成一个“测试壳”。

未来真正用起来时，它会长成这样：

```text
ReplayCase -> Backend 写入 -> Backend 读取 -> Normalize -> Compare -> Report
```

### 你未来会怎么初始化

你最终想要的调用方式，大概是这样的：

```go
harness := replayconsistency.ReplayHarness{
    Backends: []replayconsistency.Backend{inmemoryBackend, sqliteBackend},
    Cases:    replayconsistency.DefaultReplayCases(),
    Options: replayconsistency.HarnessOptions{
        LightMode: true,
        MaxCases:  10,
    },
}

report, err := harness.Run(context.Background())
if err != nil {
    panic(err)
}

blob, err := replayconsistency.MarshalReport(report)
if err != nil {
    panic(err)
}
_ = blob
```

## 实战版：先把 InMemory 和 SQLite 接起来

如果你要把它从“骨架”补成“真能回放”，我建议先只接两个后端：

1. InMemory Session 后端。
1. SQLite Session 后端。

这样最容易把问题收敛住。

### InMemory 怎么起

仓库里的 InMemory 会话服务直接用 `session/inmemory`，不用额外建库。最常见的写法是：

```go
svc := inmemory.NewSessionService(
    inmemory.WithSessionEventLimit(1000),
)
defer svc.Close()
```

如果你要带摘要能力，就再加 summarizer 相关选项；如果不带，先用默认也可以。

### SQLite 怎么起

SQLite 需要先打开一个数据库文件，再把 `*sql.DB` 传给 `sqlite.NewService(...)`。

一个最小可运行的例子如下：

```go
import (
    "database/sql"
    "os"

    _ "github.com/mattn/go-sqlite3"
    "trpc.group/trpc-go/trpc-agent-go/session/sqlite"
)

func openSQLiteService() (*sqlite.Service, func(), error) {
    file, err := os.CreateTemp("", "replay-*.db")
    if err != nil {
        return nil, nil, err
    }

    db, err := sql.Open("sqlite3", file.Name())
    if err != nil {
        _ = os.Remove(file.Name())
        return nil, nil, err
    }

    svc, err := sqlite.NewService(db)
    if err != nil {
        _ = db.Close()
        _ = os.Remove(file.Name())
        return nil, nil, err
    }

    cleanup := func() {
        _ = svc.Close()
        _ = os.Remove(file.Name())
    }

    return svc, cleanup, nil
}
```

这个写法和仓库里的 SQLite 测试风格是一致的。

## 后端应该加在哪里

如果你问“#sym:InMemory 这类后端应该加到哪里”，答案是：**加在 replay harness 的后端适配层里**，也就是 [tests/replay_consistency](tests/replay_consistency) 目录下。

我建议你把后端适配器单独放成一个子目录，例如：

- [tests/replay_consistency/backends](tests/replay_consistency/backends)
- [tests/replay_consistency/backends/inmemory.go](tests/replay_consistency/backends/inmemory.go)
- [tests/replay_consistency/backends/sqlite.go](tests/replay_consistency/backends/sqlite.go)
- [tests/replay_consistency/backends/redis.go](tests/replay_consistency/backends/redis.go)

这样做的好处是：

- harness 只负责跑 case，不关心具体后端。
- backend adapter 只负责“怎么写、怎么读、怎么关”。
- normalizer / comparator / report 也能保持独立。

### 你在 adapter 里要实现什么

对于 Session 后端，最少要理解并包装这几个能力：

- `session.Service`：创建、读取、列出、更新 session，以及写入事件、summary、state。
- `session.TrackService`：写入 track 事件。
- `session.WindowService`：如果你后面要测事件分页、窗口读取，就要看这个接口。

对于 Memory 后端，最少要理解：

- `memory.Service`：添加、更新、删除、读取、搜索记忆。

如果你后面要做 summary 的回放一致性，还要把这些内容一起纳入 adapter 的读回快照：

- `GetSessionSummaryText(...)`
- `CreateSessionSummary(...)`
- `EnqueueSummaryJob(...)`

### 最小 adapter 应该暴露什么

你可以让每个后端 adapter 实现一个很小的自定义接口，然后由 harness 调用。例如：

```go
type SessionBackend interface {
    Backend
    CreateSession(ctx context.Context, key session.Key, state session.StateMap) (*session.Session, error)
    GetSession(ctx context.Context, key session.Key) (*session.Session, error)
    UpdateSessionState(ctx context.Context, key session.Key, state session.StateMap) error
    AppendEvent(ctx context.Context, sess *session.Session, evt *event.Event) error
    CreateSessionSummary(ctx context.Context, sess *session.Session, filterKey string, force bool) error
    AppendTrackEvent(ctx context.Context, sess *session.Session, trackEvent *session.TrackEvent) error
}
```

如果你要测 Memory，就再单独做一个：

```go
type MemoryBackend interface {
    Backend
    AddMemory(ctx context.Context, userKey memory.UserKey, content string, topics []string) error
    UpdateMemory(ctx context.Context, key memory.Key, content string, topics []string) error
    DeleteMemory(ctx context.Context, key memory.Key) error
    ReadMemories(ctx context.Context, userKey memory.UserKey, limit int) ([]*memory.Entry, error)
    SearchMemories(ctx context.Context, userKey memory.UserKey, query string) ([]*memory.Entry, error)
}
```

### 先看哪些现有文件

如果你要开始写 InMemory 或 SQLite adapter，先看这些文件最划算：

- [session/inmemory/service.go](session/inmemory/service.go)
- [session/sqlite/service.go](session/sqlite/service.go)
- [session/session.go](session/session.go)
- [session/track.go](session/track.go)
- [session/summary/summary.go](session/summary/summary.go)
- [memory/inmemory/service.go](memory/inmemory/service.go)
- [memory/memory.go](memory/memory.go)

### 你做 adapter 时最少要知道什么

1. 事件怎么进 session：看 `AppendEvent(...)`。
1. state 怎么进 session：看 `UpdateSessionState(...)` 和 `StateDelta`。
1. track 怎么进 session：看 `AppendTrackEvent(...)` 和 `session.TrackEvent`。
1. summary 怎么生成和读取：看 `CreateSessionSummary(...)`、`GetSessionSummaryText(...)`。
1. memory 怎么增删改查：看 `memory.Service`。

### 推荐的接入顺序

如果你现在要真正开始做，我建议按这个顺序加：

1. 先加 `tests/replay_consistency/backends/inmemory.go`。
1. 再加 `tests/replay_consistency/backends/sqlite.go`。
1. 再把 `ReplayHarness.Run(...)` 改成真的调用这些 adapter。
1. 最后再接 Redis / Postgres / MySQL / ClickHouse。

这样你会非常清楚每个后端是怎么挂进来的，也方便以后扩展。

## 实战版：你后面应该怎么跑真实 replay

等你把 adapter 接上以后，建议按这个顺序做：

### 第 1 步：只跑轻量模式

先只跑 InMemory + SQLite。

这一步的目标不是覆盖全部后端，而是先确认：

- 事件顺序一致。
- state 最终值一致。
- summary filter-key 一致。
- track 读取一致。
- memory 写入和读取一致。

### 第 2 步：再加可选后端

后面再接 Redis、Postgres、MySQL、ClickHouse。

建议你在自己的 adapter 层里用环境变量控制是否启用，比如：

- `REPLAY_ENABLE_REDIS=1`
- `REPLAY_ENABLE_POSTGRES=1`
- `REPLAY_ENABLE_MYSQL=1`
- `REPLAY_ENABLE_CLICKHOUSE=1`

如果连接信息没填，就直接 skip，不要影响默认 CI。

### 第 3 步：把 10 条 case 跑实

这 10 条 case 不要只留定义，要真的去写、去读、去比较：

1. 单轮普通对话。
1. 多轮对话。
1. 工具调用对话。
1. State 更新。
1. Memory 写入和读取。
1. Summary 生成和更新。
1. Summary 与事件截断。
1. Track 事件。
1. 并发或乱序写入。
1. 异常恢复。

## 实战里你怎么判断结果对不对

### 正常结果

如果实现正常，你应该看到：

- 10 条 case 都能跑。
- 比较器只在真正不一致时才报 diff。
- `allowed_diff` 只出现在自动 ID、时间戳、JSON 顺序、浮点误差这类字段上。
- Summary 的 `filter_key`、`session_id`、`boundary` 只要错了就必须报错。
- `go test ./tests/replay_consistency` 可以持续通过。

### 异常结果

如果出问题，通常会体现在这几个地方：

- case 数量不对。
- state 删除后还残留。
- summary 被别的 session 覆盖。
- track 的时间顺序变了。
- memory 的 ID 变化没被归一化。
- diff 报告里定位不到具体字段路径。

## 你以后怎么扩展 case

如果你要加新的 replay case，建议按这个流程来：

1. 先在 [cases.go](tests/replay_consistency/cases.go) 里加 case 定义。
1. 再补 normalizer 里对应字段的标准化规则。
1. 再补 comparator 的字段比较。
1. 最后补 report 示例和单测。

这样不会出现“case 先加了，但没有比较逻辑”的断层。

## 实战建议

如果你是第一次做这个框架，最稳的顺序是：

1. 先把当前骨架测试跑通。
1. 再接 InMemory。
1. 再接 SQLite。
1. 再做 diff 报告更细的字段定位。
1. 再补 Redis / Postgres / MySQL / ClickHouse。

这样每一步都能验证，而且出错时很好定位。

## 最后一句话

现在你已经可以先用 `go test ./tests/replay_consistency -v` 验证骨架，然后按 InMemory 和 SQLite 的顺序把真实 adapter 接上。等你能稳定产出 diff 报告，这个框架就算进入实战版了。