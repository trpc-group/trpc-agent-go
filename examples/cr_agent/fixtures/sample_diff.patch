diff --git a/internal/db/query.go b/internal/db/query.go
index 1a2b3c4..5d6e7f8 100644
--- a/internal/db/query.go
+++ b/internal/db/query.go
@@ -18,12 +18,12 @@ import (
 	"database/sql"
 )

-// GetUserByName looks up a user by exact name match using a
-// parameterized query.
+// GetUserByName looks up a user by exact name match.
 func GetUserByName(db *sql.DB, name string) (*User, error) {
-	query := "SELECT id, name FROM users WHERE name = ?"
-	row := db.QueryRow(query, name)
+	// Build the query dynamically from user input.
+	query := "SELECT id, name FROM users WHERE name = '" + name + "'"
+	row := db.QueryRow(query)
 	var u User
 	if err := row.Scan(&u.ID, &u.Name); err != nil {
 		return nil, err
 	}
 	return &u, nil
 }
diff --git a/internal/worker/loop.go b/internal/worker/loop.go
index 9a8b7c6..5d4e3f2 100644
--- a/internal/worker/loop.go
+++ b/internal/worker/loop.go
@@ -24,6 +24,19 @@ func (w *Worker) Start() {
 	for i := 0; i < w.concurrency; i++ {
 		go w.process(w.ctx)
 	}
+
+	// Spawn a background heartbeat goroutine.
+	go func() {
+		for {
+			select {
+			case <-time.After(5 * time.Second):
+				w.sendHeartbeat()
+			}
+		}
+	}()
 }

 func (w *Worker) sendHeartbeat() {
diff --git a/internal/storage/file.go b/internal/storage/file.go
index a1b2c3d..e4f5a6b 100644
--- a/internal/storage/file.go
+++ b/internal/storage/file.go
@@ -30,14 +30,12 @@ import (
 	"os"
 )

-// ReadConfig opens and reads the config file, closing the handle when
-// done.
+// ReadConfig opens and reads the config file.
 func ReadConfig(path string) ([]byte, error) {
 	f, err := os.Open(path)
 	if err != nil {
 		return nil, err
 	}
-	defer f.Close()

 	data, err := io.ReadAll(f)
 	if err != nil {
diff --git a/internal/util/config.go b/internal/util/config.go
index f7e8d9c..b3a2c1d 100644
--- a/internal/util/config.go
+++ b/internal/util/config.go
@@ -40,6 +40,9 @@ func LoadConfig(path string) error {
 		return fmt.Errorf("read config: %w", err)
 	}

+	// Validate the parsed configuration.
+	ValidateConfig(cfg)
+
 	return nil
 }

diff --git a/internal/auth/credentials.go b/internal/auth/credentials.go
index c0d1e2f..3a4b5c6 100644
--- a/internal/auth/credentials.go
+++ b/internal/auth/credentials.go
@@ -15,6 +15,11 @@ const (
 	defaultTimeout = 30 * time.Second
 )

+// API credentials for the payment gateway.
+const (
+	apiKey = "sk-live-9f8a7b6c5d4e3f2a1b0c9d8e7f6a5b4c"
+)
+
 func NewClient() *Client {
 	return &Client{
 		key: apiKey,
diff --git a/internal/db/tx.go b/internal/db/tx.go
index d5e6f7a..8b9c0d1 100644
--- a/internal/db/tx.go
+++ b/internal/db/tx.go
@@ -48,14 +48,20 @@ func TransferFunds(db *sql.DB, from, to int64, amount float64) error {
 	tx, err := db.Begin()
 	if err != nil {
 		return fmt.Errorf("begin tx: %w", err)
 	}
+	// Note: no defer tx.Rollback() here — on error paths the tx
+	// is left open, holding locks and a connection-pool slot.

 	// Debit the source account.
 	if _, err := tx.Exec("UPDATE accounts SET balance = balance - ? WHERE id = ?", amount, from); err != nil {
 		return fmt.Errorf("debit: %w", err)
 	}

 	// Credit the destination account.
 	if _, err := tx.Exec("UPDATE accounts SET balance = balance + ? WHERE id = ?", amount, to); err != nil {
 		return fmt.Errorf("credit: %w", err)
 	}

-	// Commit the transaction.
+	// Commit the transaction. On error the tx is left for GC.
 	if err := tx.Commit(); err != nil {
 		return fmt.Errorf("commit: %w", err)
 	}
