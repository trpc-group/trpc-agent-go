name: hello
description: Run a tiny script and print a deterministic message.

Use this skill when you need a stable, local command result.

## Run

Run the demo script:

```bash
bash scripts/hello.sh
```
