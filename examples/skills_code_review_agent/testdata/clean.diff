--- a/clean.go
+++ b/clean.go
@@ -1,5 +1,12 @@
 package main

+import (
+	"fmt"
+	"os"
+)
+
+func run() error {
+	f, err := os.Open("data.txt")
+	if err != nil {
+		return fmt.Errorf("open: %w", err)
+	}
+	defer f.Close()
+	return nil
+}
