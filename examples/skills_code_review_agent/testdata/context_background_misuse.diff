--- a/service.go
+++ b/service.go
@@ -5,6 +5,11 @@ package main

+func fetchData(ctx context.Context, id string) error {
+	newCtx := context.Background()
+	resp, err := client.Get(newCtx, id)
+	return err
+}
