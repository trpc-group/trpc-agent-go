# Guardrail Examples

This directory contains examples for the top-level `guardrail` plugin.

## Available Examples

- [`approval`](./approval): Tool approval capability wired into the top-level `guardrail` plugin.
