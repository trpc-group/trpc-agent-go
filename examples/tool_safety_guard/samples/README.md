# Tool Safety Sample Corpus

These JSON files are scan-only fixtures for `examples/tool_safety_guard`.
The example CLI never executes the commands. Dangerous cases are included so the
scanner can prove pre-execution blocking, reporting, audit output, and redaction.
