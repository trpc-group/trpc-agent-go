diff --git a/file.go b/file.go
--- a/file.go
+++ b/file.go
@@ -1,0 +1,8 @@
+package main
+import "os"
+func readConfig() ([]byte, error) {
+    f, err := os.Open("config.yaml")
+    if err != nil { return nil, err }
+    return os.ReadAll(f)
+    // missing resource cleanup on f
+}
