diff --git a/command.go b/command.go
--- a/command.go
+++ b/command.go
@@ -9,6 +9,6 @@ package security
 
 import "os/exec"
 
-func Run(name string) error {
-	return exec.Command(name).Run()
+func Run(input string) error {
+	return exec.Command("sh", "-c", input).Run()
 }
