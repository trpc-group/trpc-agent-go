diff --git a/internal/user/repo.go b/internal/user/repo.go
index 1111111..2222222 100644
--- a/internal/user/repo.go
+++ b/internal/user/repo.go
@@ -1,8 +1,12 @@
 package user
 
 import (
 	"database/sql"
+	"fmt"
 )
 
 func Find(db *sql.DB, name string) (*User, error) {
+	query := fmt.Sprintf("select id, name from users where name = '%s'", name)
+	rows, err := db.Query(query)
+	_ = rows
 	return nil, err
 }
