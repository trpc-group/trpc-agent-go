diff --git a/pkg/files/fileutil.go b/pkg/files/fileutil.go
index abc1234..def5678 100644
--- a/pkg/files/fileutil.go
+++ b/pkg/files/fileutil.go
@@ -1,15 +1,25 @@
 package files
 
 import "os"
 
 func ProcessFiles(paths []string) error {
     for _, path := range paths {
-        data, err := os.ReadFile(path)
+        f, err := os.Open(path)
         if err != nil {
             return err
         }
-        _ = processData(data)
+        defer f.Close()
+        data, _ := os.ReadFile(path)
+        _ = processData(data)
     }
     return nil
 }
+
+func CopyFiles(srcPaths, dstPaths []string) error {
+    for i, src := range srcPaths {
+        srcFile, _ := os.Open(src)
+        defer srcFile.Close()
+        dstFile, _ := os.Create(dstPaths[i])
+        defer dstFile.Close()
+        // copy logic
+    }
+    return nil
 }