#!/usr/bin/env sh
set -eu

# Placeholder for organization-specific rules. Keep this script read-only and
# route it through PermissionPolicy before execution in any real workspace.
echo "custom rule hook: no organization-specific checks configured"
