# langfuse

- Make sure you have installed the Langfuse skill. The skill is open source on [GitHub](https://github.com/langfuse/skills) and can access data from the Langfuse platform.
- `.harness/observability/.env` already contains the key for accessing the test project on the Langfuse platform.
- `.harness/observability/.env` already contains access to an LLM that supports OpenAPI calls and real model invocations.
- Create test-related code under `.harness/observability/langfuse` for validation.