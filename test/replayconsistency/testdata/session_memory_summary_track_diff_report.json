{
  "mode": "lightweight",
  "baseline": "inmemory",
  "backends": [
    "inmemory",
    "sqlite",
    "redis"
  ],
  "summary": {
    "cases": 10,
    "divergences": 23,
    "allowed": 0,
    "known": 23,
    "fatal": 0,
    "unsupported": 0
  },
  "knownDivergences": [
    {
      "path": "sessions[ref=\"replay/u-state/s-state\"].state[key=\"lang\"]",
      "backend": "redis",
      "note": "An event state delta whose value is nil leaves the previous value in place instead of storing nil. session/inmemory and session/sqlite both store nil, matching session.ApplyEventStateDelta. The redis hashidx path applies deltas inside a Lua script guarded by next(stateDelta) ~= nil. Observed against miniredis, whose Lua decodes JSON null to a Lua nil; a Lua table cannot hold a nil value, so the key disappears, the table reads as empty and the guard skips the whole update. Real Redis decodes null to a cjson.null sentinel and would likely apply the delta, so this may be an emulation difference rather than a backend defect. Recorded rather than asserted until it is confirmed against a real Redis server through the integration mode."
    },
    {
      "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events",
      "backend": "redis",
      "note": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
    },
    {
      "path": "sessions[ref=\"replay/u-retry/s-retry\"].events.length",
      "backend": "redis",
      "note": "Re-appending an event with an identifier that was already stored collapses the two writes, while session/inmemory and session/sqlite keep both. The redis hashidx path stores events with HSET \u003ckey\u003e \u003ceventID\u003e \u003cjson\u003e, so a repeated identifier overwrites the field; sqlite inserts a new row with no uniqueness constraint on the event identifier, and inmemory appends unconditionally. session.Service does not document whether AppendEvent is idempotent, so neither behavior violates a stated contract, but a retry after a crash means different things depending on where the data lives."
    }
  ],
  "cases": [
    {
      "case": "single-turn",
      "description": "one user message and one assistant reply"
    },
    {
      "case": "multi-turn-ordering",
      "description": "three consecutive turns read back in append order"
    },
    {
      "case": "tool-call-round-trip",
      "description": "assistant tool call, tool response, and call metadata extensions"
    },
    {
      "case": "state-write-overwrite-clear",
      "description": "session, app and user state through write, overwrite, nil and delete",
      "divergences": [
        {
          "case": "state-write-overwrite-clear",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-state/s-state\"].state[key=\"lang\"].nil",
          "baselineValue": "true",
          "backendValue": "false",
          "allowedDiff": false,
          "known": true,
          "reason": "An event state delta whose value is nil leaves the previous value in place instead of storing nil. session/inmemory and session/sqlite both store nil, matching session.ApplyEventStateDelta. The redis hashidx path applies deltas inside a Lua script guarded by next(stateDelta) ~= nil. Observed against miniredis, whose Lua decodes JSON null to a Lua nil; a Lua table cannot hold a nil value, so the key disappears, the table reads as empty and the guard skips the whole update. Real Redis decodes null to a cjson.null sentinel and would likely apply the delta, so this may be an emulation difference rather than a backend defect. Recorded rather than asserted until it is confirmed against a real Redis server through the integration mode."
        },
        {
          "case": "state-write-overwrite-clear",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-state/s-state\"].state[key=\"lang\"].value",
          "baselineValue": "",
          "backendValue": "en",
          "allowedDiff": false,
          "known": true,
          "reason": "An event state delta whose value is nil leaves the previous value in place instead of storing nil. session/inmemory and session/sqlite both store nil, matching session.ApplyEventStateDelta. The redis hashidx path applies deltas inside a Lua script guarded by next(stateDelta) ~= nil. Observed against miniredis, whose Lua decodes JSON null to a Lua nil; a Lua table cannot hold a nil value, so the key disappears, the table reads as empty and the guard skips the whole update. Real Redis decodes null to a cjson.null sentinel and would likely apply the delta, so this may be an emulation difference rather than a backend defect. Recorded rather than asserted until it is confirmed against a real Redis server through the integration mode."
        }
      ]
    },
    {
      "case": "memory-write-update-delete",
      "description": "memory add, topic-only update, content update with identifier rotation, and delete"
    },
    {
      "case": "summary-generate-and-update",
      "description": "whole-session and branch summaries, then a regeneration that supersedes the first"
    },
    {
      "case": "summary-with-event-truncation",
      "description": "a summary covers the early turns while later turns stay as raw events"
    },
    {
      "case": "track-events",
      "description": "timing, status and error entries across two tracks"
    },
    {
      "case": "interleaved-out-of-order-writes",
      "description": "two invocations appended out of timestamp order",
      "divergences": [
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[1].branch",
          "baselineValue": "a",
          "backendValue": "b",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        },
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[1].content",
          "baselineValue": "branch a step one",
          "backendValue": "branch b step one",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        },
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[1].id",
          "baselineValue": "i1",
          "backendValue": "i2",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        },
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[1].invocationId",
          "baselineValue": "inv-a",
          "backendValue": "inv-b",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        },
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[1].offset",
          "baselineValue": "5s",
          "backendValue": "1s",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        },
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[2].content",
          "baselineValue": "branch b step one",
          "backendValue": "branch b step two",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        },
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[2].id",
          "baselineValue": "i2",
          "backendValue": "i4",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        },
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[2].offset",
          "baselineValue": "1s",
          "backendValue": "3s",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        },
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[2].stateDelta[key=\"b_steps\"]",
          "baselineValue": "\u003cabsent\u003e",
          "backendValue": "{\"key\":\"b_steps\",\"value\":\"2\"}",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        },
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[3].content",
          "baselineValue": "branch a step two",
          "backendValue": "branch a step one",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        },
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[3].id",
          "baselineValue": "i3",
          "backendValue": "i1",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        },
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[3].offset",
          "baselineValue": "9s",
          "backendValue": "5s",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        },
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[3].stateDelta[key=\"a_steps\"]",
          "baselineValue": "{\"key\":\"a_steps\",\"value\":\"2\"}",
          "backendValue": "\u003cabsent\u003e",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        },
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[4].branch",
          "baselineValue": "b",
          "backendValue": "a",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        },
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[4].content",
          "baselineValue": "branch b step two",
          "backendValue": "branch a step two",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        },
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[4].id",
          "baselineValue": "i4",
          "backendValue": "i3",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        },
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[4].invocationId",
          "baselineValue": "inv-b",
          "backendValue": "inv-a",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        },
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[4].offset",
          "baselineValue": "3s",
          "backendValue": "9s",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        },
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[4].stateDelta[key=\"a_steps\"]",
          "baselineValue": "\u003cabsent\u003e",
          "backendValue": "{\"key\":\"a_steps\",\"value\":\"2\"}",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        },
        {
          "case": "interleaved-out-of-order-writes",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-interleaved/s-interleaved\"].events[4].stateDelta[key=\"b_steps\"]",
          "baselineValue": "{\"key\":\"b_steps\",\"value\":\"2\"}",
          "backendValue": "\u003cabsent\u003e",
          "allowedDiff": false,
          "known": true,
          "reason": "Events appended out of timestamp order read back in timestamp order rather than append order. session/inmemory appends to a slice and session/sqlite inserts rows read back by insertion, so both preserve the order the caller wrote. The redis hashidx path indexes events with ZADD \u003ckey\u003e \u003cevent timestamp\u003e \u003ceventID\u003e and reads that sorted set back by score, so the timestamp decides the order. This is the reordering the issue describes: a caller that appends a late-arriving tool result carrying an earlier timestamp replays it in a different position depending on the backend. The mechanism is ordinary Redis sorted-set behavior rather than an artifact of the in-process server used by the lightweight mode."
        }
      ]
    },
    {
      "case": "retry-and-recovery",
      "description": "a replayed event, a replayed memory write and a regenerated summary after a retry",
      "divergences": [
        {
          "case": "retry-and-recovery",
          "baseline": "inmemory",
          "backend": "redis",
          "path": "sessions[ref=\"replay/u-retry/s-retry\"].events.length",
          "baselineValue": "3",
          "backendValue": "2",
          "allowedDiff": false,
          "known": true,
          "reason": "Re-appending an event with an identifier that was already stored collapses the two writes, while session/inmemory and session/sqlite keep both. The redis hashidx path stores events with HSET \u003ckey\u003e \u003ceventID\u003e \u003cjson\u003e, so a repeated identifier overwrites the field; sqlite inserts a new row with no uniqueness constraint on the event identifier, and inmemory appends unconditionally. session.Service does not document whether AppendEvent is idempotent, so neither behavior violates a stated contract, but a retry after a crash means different things depending on where the data lives."
        }
      ]
    }
  ]
}
