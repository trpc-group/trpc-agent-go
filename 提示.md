# 给初学者的 tRPC-Agent-Go 起步提示

这份文档是给刚拿到这个项目、不知道从哪里开始看的同学准备的。目标不是一次看懂全部，而是先跑起来，再按模块理解。

## 先回答你最关心的几个问题

### 1. 我应该先看哪里？

按这个顺序看最省力：

1. [README.zh_CN.md](README.zh_CN.md)
1. [docs/mkdocs/zh/index.md](docs/mkdocs/zh/index.md)
1. [docs/mkdocs/zh/runner.md](docs/mkdocs/zh/runner.md)
1. [docs/mkdocs/zh/agent.md](docs/mkdocs/zh/agent.md)
1. [examples/runner](examples/runner)

原因很简单：

- README 先告诉你这个项目能做什么。
- index.md 让你快速知道模块分工。
- runner.md 解释“程序是怎么跑起来的”。
- agent.md 解释“Agent 本身是什么”。
- examples/runner 是最适合新手的第一个可运行例子。

### 2. 我应该先跑什么？

如果你只是想先有一个能跑的结果，建议先跑 [examples/runner](examples/runner) 里的示例。

最基本的步骤是：

1. 安装 Go 1.21 或更高版本。
1. 准备一个兼容 OpenAI 的模型 API Key。
1. 进入示例目录运行 `go run .`。

如果你暂时没有 API Key，也可以先只看代码结构，先理解 Runner、Agent、Session、Memory 的关系，再回来跑示例。

### 3. 这个项目到底在做什么？

你可以把它理解成一个“Go 版 Agent 基础设施”。它主要做四件事：

- 接收用户消息。
- 让 Agent 去思考、调用工具、输出结果。
- 保存会话、状态、记忆和摘要。
- 把整个过程做成可观测、可测试、可扩展的系统。

如果只用一句话概括：它不是单纯的“聊天机器人库”，而是一套能支撑生产环境的 Agent 框架。

### 4. 为什么我一开始会看不懂？

因为这个仓库不是一个小脚本，而是一个完整框架，里面有很多层：

- `agent` 负责推理和生成。
- `runner` 负责把一次请求串起来。
- `session` 负责保存会话、事件、状态、摘要和 track。
- `memory` 负责长期记忆。
- `tool` 负责工具调用。

新手最容易犯的错，是一上来就想看懂全部目录。正确顺序是先看请求怎么流转，再看数据怎么保存。

## 推荐学习路线

### 第 1 步：先理解整体流程

先看 [docs/mkdocs/zh/index.md](docs/mkdocs/zh/index.md) 里的模块说明，再看 [docs/mkdocs/zh/runner.md](docs/mkdocs/zh/runner.md)。

你要先建立这条主线：

```text
用户消息 -> Runner -> Agent -> Tool/Model -> Event -> Session/Memory
```

只要这条线清楚了，后面的很多目录都会好理解很多。

### 第 2 步：跑一个最小示例

建议直接看 [examples/runner](examples/runner) 的示例。你可以把它当成项目的“Hello World”。

这个示例能帮你看懂：

- 怎么创建模型。
- 怎么创建 Agent。
- 怎么创建 Runner。
- 怎么发送一条消息。
- 怎么读取流式事件。

### 第 3 步：再看 Session 和 Memory

如果你关心“为什么它能记住上一次聊天”，就看这些文档：

- [docs/mkdocs/zh/session.md](docs/mkdocs/zh/session.md)
- [docs/mkdocs/zh/memory.md](docs/mkdocs/zh/memory.md)

先理解两个概念就够了：

- Session：保存一段对话的事件、状态和摘要。
- Memory：保存跨会话的长期记忆。

### 第 4 步：最后再看更复杂的功能

如果你已经能跑示例，再继续看：

- Graph： [docs/mkdocs/zh/graph.md](docs/mkdocs/zh/graph.md)
- Tool： [docs/mkdocs/zh/tool.md](docs/mkdocs/zh/tool.md)
- 可观测性： [docs/mkdocs/zh/observability.md](docs/mkdocs/zh/observability.md)
- A2A： [docs/mkdocs/zh/a2a-interaction.md](docs/mkdocs/zh/a2a-interaction.md)
- Skills： [docs/mkdocs/zh/skill.md](docs/mkdocs/zh/skill.md)

## 我建议你怎么读代码

### 不要先从根目录乱翻

初学者最适合按“入口 -> 执行流程 -> 存储层 -> 示例”的顺序看代码。

推荐顺序是：

1. [runner/runner.go](runner/runner.go)
1. [agent/llmagent](agent/llmagent)
1. [session/session.go](session/session.go)
1. [session/inmemory](session/inmemory)
1. [memory/inmemory](memory/inmemory)
1. [examples/runner](examples/runner)

### 先只看主干，不要一开始钻细节

你第一轮只需要回答这三个问题：

- 一次请求从哪里进来？
- 事件是怎么流出去的？
- 状态和摘要是在哪里保存的？

等这三个问题清楚了，再去看 Redis、Postgres、MySQL、ClickHouse 这些具体后端实现。

## 对新手最实用的理解方式

### 1. Runner 是“调度器”

它负责把一次请求串起来，帮你处理会话、事件流和上下文传递。

### 2. Agent 是“思考和执行的核心”

它负责决定要不要调用工具、要不要继续推理、最后怎么回答。

### 3. Session 是“短期记忆”

它保存同一个会话里的历史事件、状态、摘要和 track。

### 4. Memory 是“长期记忆”

它保存跨会话、可复用的用户偏好、事实和经验。

### 5. Tool 是“外部能力”

它让 Agent 可以查数据、算数、搜索、调用 MCP 服务、读写文件等。

## 如果你想最快上手，建议这样做

### 第一轮：只跑示例

先把 [examples/runner](examples/runner) 跑通，确认你本机的 Go 环境和模型配置没问题。

### 第二轮：改一个最小功能

比如：

- 把示例里的模型名换成你自己的。
- 改一条系统提示词。
- 加一个简单工具，比如加法器。

### 第三轮：看会话和记忆

再试着理解：

- 为什么第二轮对话还能记住第一轮内容。
- state 是怎么保存的。
- summary 是什么时候生成的。

### 第四轮：再碰复杂功能

等你理解了基础流程，再去看 Graph、Skills、A2A、AG-UI、Telemetry。

## 常见困惑

### 看到很多目录，不知道从哪开始怎么办？

先只看 [README.zh_CN.md](README.zh_CN.md) 和 [examples/runner](examples/runner)。不要一开始想把整个仓库都看懂。

### 我是 Go 初学者，能看懂吗？

可以。先只抓住 4 个关键词：Runner、Agent、Session、Memory。其他内容都先当作扩展能力。

### 没有模型 Key 还能学吗？

可以。你仍然可以先看文档、看示例、看测试，再决定接什么模型。

### 我最该关注哪些文件？

优先看这几个：

- [README.zh_CN.md](README.zh_CN.md)
- [docs/mkdocs/zh/index.md](docs/mkdocs/zh/index.md)
- [docs/mkdocs/zh/runner.md](docs/mkdocs/zh/runner.md)
- [docs/mkdocs/zh/agent.md](docs/mkdocs/zh/agent.md)
- [session/session.go](session/session.go)
- [runner/runner.go](runner/runner.go)
- [examples/runner](examples/runner)

## 一句话建议

先跑 [examples/runner](examples/runner)，再读 [runner/runner.go](runner/runner.go)，然后看 [session/session.go](session/session.go)。

如果你按这个顺序学，成本最低，收获最快。



## 如果你要完成这个 replay consistency issue

如果你的目标不是“先了解项目”，而是“直接把 session / memory / summary replay harness 做出来”，建议你按下面顺序看。

### 先看哪些文件

1. [session/session.go](session/session.go)
1. [session/track.go](session/track.go)
1. [session/summary/summary.go](session/summary/summary.go)
1. [session/summary/report.go](session/summary/report.go)
1. [runner/runner.go](runner/runner.go)
1. [session/inmemory/service.go](session/inmemory/service.go)
1. [session/sqlite/service.go](session/sqlite/service.go)
1. [session/redis/service.go](session/redis/service.go)
1. [session/postgres/service.go](session/postgres/service.go)
1. [session/mysql/service.go](session/mysql/service.go)
1. [session/clickhouse/service.go](session/clickhouse/service.go)
1. [memory/memory.go](memory/memory.go)
1. [memory/inmemory/service.go](memory/inmemory/service.go)
1. [memory/redis/service.go](memory/redis/service.go)

### 先理解哪些能力

你要先搞清楚这几件事，再开始写 harness：

- Session 里有哪些可比内容：event、state、tracks、summaries。
- Summary 的 filter-key 语义是什么，哪里会生成、保存和读取。
- Track event 是怎么挂到 session state 里的。
- Memory 的 add、update、delete、read、search 分别怎么用。
- 不同后端哪些能力是完全一致的，哪些只能标记为 unsupported 或 allowed_diff。

### 推荐实现顺序

1. 先做统一的数据模型和规范化器，只处理 event、state、memory、summary、track 的标准化输出。
1. 再做最小可运行后端适配器，先接 InMemory 和 SQLite。
1. 然后补比较器和 diff report 生成器。
1. 再补 10 条公开 replay case。
1. 最后才接 Redis、Postgres、MySQL、ClickHouse 这些可选后端。

### 你应该重点看的测试

这些测试最适合帮你理解“真实行为应该是什么”：

- [session/inmemory](session/inmemory)
- [session/sqlite](session/sqlite)
- [session/redis](session/redis)
- [session/postgres](session/postgres)
- [session/mysql](session/mysql)
- [session/clickhouse](session/clickhouse)
- [session/summary](session/summary)
- [memory/inmemory](memory/inmemory)
- [memory/redis](memory/redis)

### 你最终要交付什么

这个 issue 最后应该落成三类东西：

- 一个可复用的 replay harness 目录，比如 `session/replaytest/` 或 `tests/replay_consistency/`。
- 一份差异报告示例，比如 `session_memory_summary_track_diff_report.json`。
- 一组可跑的测试，确保归一化器、比较器和报告生成器都能单独验证。

### 对新手最重要的一点

不要一开始就想着”把所有后端都接完”。正确顺序是：

1. 先让 InMemory + SQLite 跑通。
1. 再做比较器和报告。
1. 再补可选后端。
1. 最后才调精度、allowed_diff 和异常恢复用例。

---

---

## 给纯新手：从零开始完成这个 replay harness 的实操指南

以下内容假设你只有一点点 Go 基础，知道怎么跑 go run、怎么写一个 struct 和方法，但对这个项目完全陌生。目标是把上面这个 “Session / Memory / Summary replay harness” 从现有骨架变成真正能跑的测试工具。

---

### 第一步：理解你现在拥有的东西

打开 [tests/replay_consistency/](tests/replay_consistency/) 目录，里面有 9 个文件。它们组成了一个**骨架（skeleton）**——框架已经搭好，但最核心的执行逻辑还没写。

**你已经有的 4 层东西：**

| 层 | 文件 | 做什么 | 是否完成 |
|---|---|---|---|
| 数据模型 | [types.go](tests/replay_consistency/types.go) | 定义了 ReplayCase、Operation、Diff、Backend 等所有类型 | ✅ 已完成 |
| 测试用例 | [cases.go](tests/replay_consistency/cases.go) | 定义了 10 个标准 replay case（单轮对话、多轮对话、工具调用、state 更新等） | ✅ 已完成，覆盖了任务要求的全部 10 类场景 |
| 规范化器 | [normalize.go](tests/replay_consistency/normalize.go) | 把 Event、State、Memory、Summary、Track 转成可比较的统一格式 | ✅ 已完成 |
| 比较器 | [compare.go](tests/replay_consistency/compare.go) | 两个 NormalizedSnapshot 逐字段对比，生成 Diff 列表 | ✅ 已完成 |
| 报告器 | [report.go](tests/replay_consistency/report.go) | 把 Diff 列表汇总成 Report 并输出 JSON | ✅ 已完成 |
| 编排器 | [harness.go](tests/replay_consistency/harness.go) | 这个**还没写**——它应该遍历 case、遍历后端、执行操作、读出结果、调用比较器 | ❌ 只有骨架 |

也就是说，你要做的最核心工作就是**把 harness.go 里的骨架替换成真正能调用 session.Service 和 memory.Service 的逻辑**。

---

### 第二步：理解你要对接的两个核心接口

在开始写代码之前，你必须非常清楚地理解这两个 interface。每一个后端（InMemory、SQLite、Redis、Postgres、MySQL、ClickHouse）都实现了同样的接口，所以你只需要写一套调用代码。

#### 2.1 session.Service 接口

定义位置：[session/session.go:1048](session/session.go#L1048)

```go
type Service interface {
    CreateSession(ctx context.Context, key Key, state StateMap, options ...Option) (*Session, error)
    GetSession(ctx context.Context, key Key, options ...Option) (*Session, error)
    ListSessions(ctx context.Context, userKey UserKey, options ...Option) ([]*Session, error)
    DeleteSession(ctx context.Context, key Key, options ...Option) error
    AppendEvent(ctx context.Context, session *Session, event *event.Event, options ...Option) error
    UpdateAppState(ctx context.Context, appName string, state StateMap) error
    DeleteAppState(ctx context.Context, appName string, key string) error
    ListAppStates(ctx context.Context, appName string) (StateMap, error)
    UpdateUserState(ctx context.Context, userKey UserKey, state StateMap) error
    ListUserStates(ctx context.Context, userKey UserKey) (StateMap, error)
    DeleteUserState(ctx context.Context, userKey UserKey, key string) error
    UpdateSessionState(ctx context.Context, key Key, state StateMap) error
    CreateSessionSummary(ctx context.Context, sess *Session, filterKey string, force bool) error
    EnqueueSummaryJob(ctx context.Context, sess *Session, filterKey string, force bool) error
    GetSessionSummaryText(ctx context.Context, sess *Session, opts ...SummaryOption) (string, bool)
    Close() error
}
```

**你需要用到的几个关键方法（按 replay case 场景对应）：**

- **AppendEvent**: 往 session 追加事件。对应单轮/多轮对话、工具调用、并发写入、异常恢复等 case。
- **UpdateSessionState / UpdateAppState / UpdateUserState**: 修改 state。对应 state 更新 case。
- **CreateSessionSummary / GetSessionSummaryText**: 创建和读取摘要。对应 summary 相关 case。
- **AppendTrackEvent**: 追加 track 事件。对应 track event case。（注意：这个方法定义在 Session 结构体上，不是 Service 接口的方法，但 Session 在 GetSession 时会带出 Tracks。）

#### 2.2 memory.Service 接口

定义位置：[memory/memory.go:174](memory/memory.go#L174)

```go
type Service interface {
    Reader  // 嵌入了 ReadMemories + SearchMemories
    AddMemory(ctx context.Context, userKey UserKey, memory string, topics []string, opts ...AddOption) error
    UpdateMemory(ctx context.Context, memoryKey Key, memory string, topics []string, opts ...UpdateOption) error
    DeleteMemory(ctx context.Context, memoryKey Key) error
    ClearMemories(ctx context.Context, userKey UserKey) error
    Tools() []tool.Tool
    EnqueueAutoMemoryJob(ctx context.Context, sess *session.Session) error
    Close() error
}

type Reader interface {
    ReadMemories(ctx context.Context, userKey UserKey, limit int) ([]*Entry, error)
    SearchMemories(ctx context.Context, userKey UserKey, query string, opts ...SearchOption) ([]*Entry, error)
}
```

**你需要用的方法：**
- **AddMemory**: 写入一条记忆。对应 memory 写入 case。
- **UpdateMemory**: 更新记忆。对应 memory 更新 case。
- **ReadMemories**: 读取记忆列表。用于 “读回验证”。
- **SearchMemories**: 搜索记忆。用于验证搜索后的结果。

#### 2.3 关键数据结构速查

**session.Session** ([session/session.go:63](session/session.go#L63))：
```
Session.Events      → []event.Event   // 会话中的所有事件
Session.State       → StateMap        // 即 map[string][]byte
Session.Tracks      → map[Track]*TrackEvents  // track 事件按 track 名分组
Session.Summaries   → map[string]*Summary      // 按 filterKey 分组
```

**event.Event** ([event/event.go:99](event/event.go#L99))：
```
Event.Response       → *model.Response  // 嵌入了 LLM 响应（Choices）
Event.Author         → string           // “user” 或 “assistant” 或 “tool”
Event.ID             → string           // 事件唯一 ID
Event.Timestamp      → time.Time
Event.Branch         → string           // agent 执行链路
Event.Tag            → string           // 业务标签
Event.FilterKey      → string           // 分层事件过滤 key
Event.StateDelta     → map[string][]byte  // 事件附带的 state 变更
Event.Extensions     → map[string]json.RawMessage // 扩展信息（如 tool call args）
```

**memory.Entry** ([memory/memory.go:239](memory/memory.go#L239))：
```
Entry.ID        → string
Entry.AppName   → string
Entry.UserID    → string
Entry.Memory    → *Memory  // 内嵌的 Memory 结构有 Content、Topics、Kind 等
Entry.UpdatedAt → time.Time
Entry.Score     → float64  // 向量搜索结果才有
```

---

### 第三步：理解每个 Replay Case 到底要做什么

[types.go](tests/replay_consistency/types.go) 中定义的 `Operation` 是核心。一个 ReplayCase 是一组 Operation 的有序列表。每种 Operation 对应你需要在 harness 里执行的一个动作：

| Operation.Kind | 你要调用的后端方法 | 关键参数来源 |
|---|---|---|
| `append_event` | `sessionService.AppendEvent(ctx, sess, event)` | `Operation.Event` |
| `update_state` | `sessionService.UpdateSessionState(ctx, key, statePatch)` | `Operation.StatePatch` |
| `delete_state` | 按 key 逐条调用 `sess.DeleteState(...)` 或通过 state 写 nil | `Operation.StateDelete` |
| `clear_state` | 把所有 state key 置为 nil 或空 | `Operation.ClearState` |
| `add_memory` | `memoryService.AddMemory(ctx, userKey, content, topics)` | `Operation.MemoryAdd` |
| `update_memory` | `memoryService.UpdateMemory(ctx, memoryKey, content, topics)` | `Operation.MemoryUpdate` |
| `delete_memory` | `memoryService.DeleteMemory(ctx, memoryKey)` | `Operation.MemoryDelete` |
| `create_summary` | `sessionService.CreateSessionSummary(ctx, sess, filterKey, force)` | `Operation.Summary`, `Operation.FilterKey` |
| `append_track_event` | `sess.AppendTrackEvent(trackEvent)` | `Operation.Track` |
| `read_back` | `sessionService.GetSession(ctx, key)` / `memoryService.ReadMemories(ctx, userKey, limit)` | 不需要额外参数，读回当前状态 |

**理解 “read_back” 的含义**：每个 replay case 执行完所有 Operation 后，你需要：
1. 对 session 后端：调用 `GetSession` 获取最终的 Session 对象
2. 对 memory 后端：调用 `ReadMemories` 或 `SearchMemories` 获取最终的 Memory 列表
3. 把读取到的数据投给 normalize + compare 流水线

---

### 第四步：理解什么是 “baseline”，什么是 “actual”

这是一个**跨后端一致性测试**。核心思想是：
- **baseline** = 把同一组 Operation 在 InMemory 后端上执行，读回来的结果
- **actual** = 把同一组 Operation 在另一个后端（如 SQLite）上执行，读回来的结果
- 比较 baseline 和 actual 是否一致

但也可以：
- **baseline** = 你预定义的预期值（golden snapshot）
- **actual** = 任意后端执行后的结果

现有的 compare.go 选的是第一种策略。你用 `HarnessOptions.BaselineBackend` 指定哪个后端作为基准。

---

### 第五步：具体实现 plan

现在你知道所有上下文了，接下来做这 5 件事：

#### 5.1 实现 Backend 接口——对接真实后端

[types.go](tests/replay_consistency/types.go) 定义了 `Backend` 接口：

```go
type Backend interface {
    Name() string
    Kind() BackendKind
    Supports(feature string) bool
    Close() error
}
```

你需要为每一类后端创建一个 adapter struct，它不光实现这 4 个方法，还要**持有真实的 `session.Service` 或 `memory.Service` 实例**，让你能在 harness 里调用。

**推荐结构：**

```go
// SessionBackend wraps a session.Service for replay testing.
type SessionBackend struct {
    name string
    svc  session.Service
}

func (b *SessionBackend) Name() string          { return b.name }
func (b *SessionBackend) Kind() BackendKind      { return BackendKindSession }
func (b *SessionBackend) Close() error           { return b.svc.Close() }
func (b *SessionBackend) Supports(f string) bool { /* 根据 name 返回 true/false */ }

// 额外暴露真实 service
func (b *SessionBackend) SessionService() session.Service { return b.svc }
```

同样为 Memory 后端做一个 `MemoryBackend`。

**工厂函数：**

```go
func NewInMemorySessionBackend() *SessionBackend {
    return &SessionBackend{
        name: “inmemory”,
        svc:  inmemory.NewSessionService(),
    }
}

func NewSQLiteSessionBackend(dsn string) (*SessionBackend, error) {
    svc, err := sqlite.NewService(dsn)
    if err != nil {
        return nil, err
    }
    return &SessionBackend{name: “sqlite”, svc: svc}, nil
}
```

同理为 memory 写工厂：
- memory/inmemory → `inmemory.NewMemoryService()`
- memory/redis 等可选后端

#### 5.2 实现 Operation 执行器

在 harness.go 里，不要像现在这样直接返回 skeleton diff。你需要写一个真实的执行函数：

```go
func executeOperations(
    ctx context.Context,
    sessionSvc session.Service,
    memorySvc memory.Service,
    ops []Operation,
    appName, userID, sessionID string,
) (*session.Session, []*memory.Entry, error) {
    // 1. 先创建 session
    key := session.Key{AppName: appName, UserID: userID, SessionID: sessionID}
    sess, err := sessionSvc.CreateSession(ctx, key, nil)
    if err != nil {
        return nil, nil, err
    }

    for _, op := range ops {
        switch op.Kind {
        case OperationKindAppendEvent:
            sessionSvc.AppendEvent(ctx, sess, op.Event)
        case OperationKindUpdateState:
            sessionSvc.UpdateSessionState(ctx, key, op.StatePatch)
        case OperationKindDeleteState:
            for _, k := range op.StateDelete {
                sess.DeleteState(k)
            }
        case OperationKindAddMemory:
            userKey := memory.UserKey{AppName: op.MemoryAdd.UserKey.AppName, UserID: op.MemoryAdd.UserKey.UserID}
            memorySvc.AddMemory(ctx, userKey, op.MemoryAdd.Content, op.MemoryAdd.Topics)
        case OperationKindUpdateMemory:
            memKey := memory.Key{AppName: op.MemoryUpdate.UserKey.AppName, UserID: op.MemoryUpdate.UserKey.UserID, MemoryID: op.MemoryUpdate.MemoryID}
            memorySvc.UpdateMemory(ctx, memKey, op.MemoryUpdate.Content, op.MemoryUpdate.Topics)
        case OperationKindCreateSummary:
            sessionSvc.CreateSessionSummary(ctx, sess, op.FilterKey, false)
        case OperationKindAppendTrackEvent:
            sess.AppendTrackEvent(op.Track)
        // ... 其他 case
        }
    }

    // 2. 读回最终状态
    finalSess, err := sessionSvc.GetSession(ctx, key)
    if err != nil {
        return nil, nil, err
    }
    memories, err := memorySvc.ReadMemories(ctx, memory.UserKey{AppName: appName, UserID: userID}, 100)
    if err != nil {
        // 如果某后端不支持 ReadMemories，不一定是致命错误，
        // 可以标记为 unsupported
    }

    return finalSess, memories, nil
}
```

#### 5.3 把 execution 结果转成 NormalizedSnapshot

你已经有了 `NormalizeEvent`、`NormalizeState`、`NormalizeMemoryEntry`、`NormalizeSummary`、`NormalizeTrackEvent` 这些函数。你需要写一个聚合函数：

```go
func snapshotFromSession(sess *session.Session, entries []*memory.Entry) NormalizedSnapshot {
    snap := NormalizedSnapshot{}
    for i, evt := range sess.Events {
        ne := NormalizeEvent(&evt)
        ne.Index = i
        snap.Events = append(snap.Events, ne)
    }
    snap.State = NormalizeState(sess.State)
    for _, e := range entries {
        snap.Memories = append(snap.Memories, NormalizeMemoryEntry(e))
    }
    for filterKey, sum := range sess.Summaries {
        snap.Summaries = append(snap.Summaries, NormalizeSummary(sess.ID, filterKey, sum))
    }
    for _, track := range sess.Tracks {
        for _, te := range track.Events {
            snap.Tracks = append(snap.Tracks, NormalizeTrackEvent(&te))
        }
    }
    return snap
}
```

#### 5.4 把主干跑通：先只对接 InMemory + SQLite

1. 创建两个 SessionBackend：InMemory 和 SQLite
2. 对每个 replay case：
   - 在两个后端上分别执行同一组 Operation
   - 分别得到 NormalizedSnapshot
   - 以 InMemory 的 snapshot 为 baseline，调用 `CompareSnapshots(...)`
   - 收集 diff
3. 调用 `BuildReport(results)` 生成最终报告
4. 输出 JSON 到文件

状态检查点：如果 `single_turn_text` 在两边的 events 内容和 state 完全一致，你的第一个 case 就通过。你大概率会遇到的问题：
- **event ID 不一致** → 这些已经在 compare.go 里标记为 `allowed: true`
- **timestamp 不一致** → 已经标记为 `allowed: true`
- **SQLite 返回的 state value 和 InMemory 不同** → 检查是否是 json 序列化/反序列化带来的 key 顺序问题，normalize.go 里的 `canonicalizeRawJSON` 已经处理了这个

#### 5.5 补 Supports 方法和 unsupported 标记

不是所有后端都支持所有功能。你需要搞清楚哪些后端支持什么：

| 能力 | InMemory | SQLite | Redis | Postgres | MySQL | ClickHouse |
|---|---|---|---|---|---|---|
| 基础 event CRUD | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| State 更新 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Event 分页 (EventPage) | ❌ | ❌ | ❌ | ✅ | ✅ | ❌ |
| Summary 存储 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Track 事件 | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ |
| TTL 支持 | ✅ | ❌ | ✅ | ❌ | ❌ | ❌ |
| Event Search (向量) | ❌ | ❌ | ❌ | ✅ | ❌ | ❌ |

在 `Supports` 方法里按 feature 名称返回 true/false，并在 harness 里遇到不支持的功能时，生成一个 `allowed: true` 的 diff 并标注 `explanation: “backend X does not support Y”`。

---

### 第六步：处理规范化（归一化）的细节

任务描述要求对以下差异做归一化。大部分已经在 [normalize.go](tests/replay_consistency/normalize.go) 里实现了：

| 差异来源 | 现有处理 | 是否足够 |
|---|---|---|
| 自动生成 ID（event ID, memory ID） | compare.go 标记为 allowed | ✅ 足够 |
| 时间戳 | `time.RFC3339Nano` 转 UTC | ✅ 足够 |
| JSON 字段顺序 | `canonicalizeRawJSON` 先 unmarshal 再 marshal | ✅ 足够 |
| map 遍历顺序 | `sort.SliceStable` 按 key 排序 | ✅ 足够 |
| 后端私有 metadata | `NormalizedMemory.Metadata` 单独提取 | ✅ 足够 |
| 浮点相似度（Score/duration） | `stableFloat` 固定 4 位小数 | ⚠️ 可能需要加强——不同后端的 score 可能有本质差异 |
| Track 事件的 type 字段 | compare.go 标记 allowed | ✅ 足够 |

**你可能需要额外处理的：**
- 跨后端 Memory ID 不同：InMemory 后端的 `AddMemory` 可能自动生成 ID，导致同一份 memory 在两个后端 ID 完全不一样。你需要在比较时按 **content + topics** 匹配，而不是按 ID 匹配。
- Summary 的 `UpdatedAt` 时间漂移：如果 summary 是执行时生成的（而非预写入的），两个后端的时间会有微小差异。
- 并发写入 case（`interleaved_concurrency`）：目前的 case 只是模拟了不同 branch 的事件追加，但没有真正用 goroutine 并发写。如果你要真正模拟，需要在 harness 里加并发控制。

---

### 第七步：按什么顺序看代码最合理

不用从头翻到尾。按这个顺序看，每个文件只看你需要的那部分：

| 顺序 | 文件 | 看什么 | 目的 |
|---|---|---|---|
| 1 | [tests/replay_consistency/types.go](tests/replay_consistency/types.go) | 全部 126 行 | 理解所有类型定义 |
| 2 | [tests/replay_consistency/cases.go](tests/replay_consistency/cases.go) | 重点看 `DefaultReplayCases()` 和 helper 函数 | 理解 10 个 case 的数据结构 |
| 3 | [session/session.go](session/session.go) | 只看 `type Session struct`、`type Summary struct`、`type Service interface` | 理解 session 的字段和接口 |
| 4 | [session/track.go](session/track.go) | 全部 82 行 | 理解 Track 是怎么存到 session 里的 |
| 5 | [memory/memory.go](memory/memory.go) | 只看 `type Service interface`、`type Entry struct`、`type Memory struct` | 理解 memory 的字段和接口 |
| 6 | [tests/replay_consistency/normalize.go](tests/replay_consistency/normalize.go) | 全部 | 理解每一种数据怎么被标准化 |
| 7 | [tests/replay_consistency/compare.go](tests/replay_consistency/compare.go) | 全部 | 理解比较逻辑和哪些字段是 allowed_diff |
| 8 | [session/inmemory/service.go](session/inmemory/service.go) | 重点看 `AppendEvent`、`GetSession`、`UpdateSessionState`、`CreateSessionSummary` | 理解 InMemory 后端怎么工作的，作为参考实现 |
| 9 | [session/sqlite/service.go](session/sqlite/service.go) | 同上几个方法 | 理解 SQLite 后端的差异 |
| 10 | [memory/inmemory/service.go](memory/inmemory/service.go) | 重点看 `AddMemory`、`ReadMemories`、`SearchMemories` | 理解 InMemory 后端怎么工作的 |

---

### 第八步：常见错误和避免方式

**错误 1：直接比较 `session.Session` 对象**
❌ `if reflect.DeepEqual(sess1, sess2)`  
✅ 走 normalize → compare 流水线，因为时间戳、ID、JSON 顺序本来就不一样

**错误 2：忘记创建 session 就直接 AppendEvent**
有些后端（特别是 SQLite）要求 session 必须先存在于数据库里。先用 `CreateSession` 创建。

**错误 3：state 操作的 scope 混淆**
- `UpdateSessionState(key SessionKey)` → session 级别 state
- `UpdateUserState(userKey UserKey)` → user 级别 state  
- `UpdateAppState(appName)` → app 级别 state

如果你的 case 用 OperationKindUpdateState，要根据你要测试的 scope 选对方法。

**错误 4：track 事件的保存方式**
Track 不是直接调用 `sessionService` 上的方法，而是：
1. 先 `GetSession` 拿到 session 对象
2. 调用 `sess.AppendTrackEvent(event)`
3. 再通过某种方式持久化回去（或者依赖框架自动同步）

**错误 5：Memory 的 ID 生成**
InMemory backend 的 `AddMemory` 会调用 `uuid.NewString()` 给你生成一个 ID。同一个 content 写到两个后端，ID 完全不一样。比较时必须按 content+user 匹配。

---

### 第九步：可运行的伪代码——整个 harness 的完整骨架

以下是 harness.go 应该演变成的样子（概念代码，不是复制粘贴）：

```go
func (h *ReplayHarness) Run(ctx context.Context) (Report, error) {
    if len(h.Cases) == 0 {
        h.Cases = DefaultReplayCases()
    }
    if h.Options.MaxCases > 0 && h.Options.MaxCases < len(h.Cases) {
        h.Cases = h.Cases[:h.Options.MaxCases]
    }

    // 分离 session 和 memory backend
    sessionBackends := filterBackends(h.Backends, BackendKindSession)
    memoryBackends  := filterBackends(h.Backends, BackendKindMemory)

    // 选 baseline（默认：第一个 session/memory backend）
    baselineSession := sessionBackends[0]
    baselineMemory  := memoryBackends[0]

    var results []CaseResult

    for _, replayCase := range h.Cases {
        // 1) 在 baseline 后端上执行所有 operation
        baselineSess, baselineMems, err := executeReplay(
            ctx, baselineSession, baselineMemory, replayCase.Operations,
        )
        if err != nil {
            // case 失败，记录 error 继续下一个
            continue
        }
        baselineSnapshot := buildSnapshot(baselineSess, baselineMems)

        // 2) 在每个被测后端上执行同一组 operation
        for _, sessBackend := range sessionBackends {
            memBackend := matchingMemory(memoryBackends, sessBackend)
            actualSess, actualMems, execErr := executeReplay(
                ctx, sessBackend, memBackend, replayCase.Operations,
            )
            // 处理 execErr（标记 unsupported 等）
            actualSnapshot := buildSnapshot(actualSess, actualMems)

            // 3) 比较
            diffs := CompareSnapshots(
                replayCase.Name, sessBackend.Name(),
                baselineSnapshot, actualSnapshot,
            )

            // 4) 记录结果
            results = append(results, CaseResult{
                CaseName: replayCase.Name,
                Backend:  sessBackend.Name(),
                Diffs:    diffs,
            })
        }
    }

    return BuildReport(results), nil
}
```

---

### 第十步：检验你是否真正理解了这个任务

如果你能回答以下问题，就可以开始写代码：

1. 一个 `ReplayCase` 里有多个 `Operation`，执行顺序是确定的还是任意的？（答案：按数组顺序依次执行）
2. “read_back” 这个操作类型在 Operation 的 switch-case 里需要单独处理吗？（答案：不需要。read_back 是在所有 Operation 执行完后统一做的 GetSession + ReadMemories，不是一个独立的 Operation step）
3. 为什么 memory 的 ID 比较被标记为 `allowed: false`（不允许差异），但实际上 InMemory 和 SQLite 的 memory ID 一定不同？（答案：这里需要在比较前做 **按 content+topics 重新匹配**——这是一个你可能需要自己增强的地方）
4. `canonicalizeRawJSON` 做了什么？为什么需要它？（答案：它把 JSON 字节先反序列化成 Go 的 map/slice，再序列化回来。这样 `{“b”:2,”a”:1}` 和 `{“a”:1,”b”:2}` 就变得一样了）

---

### 一句话行动清单

1. 看 [tests/replay_consistency/](tests/replay_consistency/) 全部 9 个文件的代码
2. 为 session 和 memory 各写一个 Backend adapter（包装真实 Service）
3. 为 memory/inmemory 写 Backend wrapper
4. 重写 [harness.go](tests/replay_consistency/harness.go) 的 Run 方法，真正执行 Operation、读回结果、调用 compare
5. 跑通 InMemory + SQLite（session）+ InMemory（memory）的完整流程
6. 修掉所有非 allowed_diff 的差异
7. 生成 `session_memory_summary_track_diff_report.json`
8. 可选：补 Redis、Postgres、MySQL、ClickHouse backend